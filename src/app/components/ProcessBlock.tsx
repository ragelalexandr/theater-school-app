// Файл: src/components/ProcessBlock.tsx

import React from 'react';

const ProcessBlock = () => {
  return (
    <div
      style={{
        width: '1920px',
        height: '900px',
        backgroundColor: '#000000', // Укажите цвет фона, если нужно (можно заменить)
        position: 'absolute',
        top: '1800px',
      }}
    >
      {/* Здесь можно добавить контент */}
    </div>
  );
};

export default ProcessBlock;
